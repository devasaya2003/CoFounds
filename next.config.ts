/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "cryptologos.cc",
      "lh3.googleusercontent.com",
    ],
  },
};

export default nextConfig;
